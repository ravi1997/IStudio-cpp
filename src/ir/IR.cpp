#include "ir/IR.h"

namespace istudio::ir {

// IR implementation is header-only for now
// This allows for flexible extension and avoids linking issues
// Specific instruction types can be added as needed

} // namespace istudio::ir